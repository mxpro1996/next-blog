---
title: "STM32笔记与教训 (尽信书，不如无书)"
date: 2024-07-08T10:28:36+08:00
draft: false
---

## 1. TBD
* [ ] DualDMA + RTOS/LVGL/LWIP
* [ ] RFID/CAN/TLSF
* [ ] Player + AI/Watch
* [ ] Linux/OpenCV + QT/Slint
* [ ] PCB1 (F1C200S + RV1103)
* [ ] PCB2 (LT8619C + LT6911)
* [ ] PCB3 (Sunxi-A13/RK3399)
* [ ] RaspPico (mpy) + ESP32
---
* [x] SquareLine / ARM2D
* [x] MQTT + cJSON
* [x] IWDG + WWDG + BKP REGs
* [x] PWR + WKUP (Sleep/Stop/Standby)
* [x] ADC/DAC (TRGO/MultiADC/Chans)
* [x] Flash + EPROM
* [x] SDIO + USB Bulk(Block W/R)
* [x] FATFS + W25Q128/SDCard/UDisk
* [x] UDisk + USBHost_Process
* [x] Key/Mouse USB-HID Device 
* [x] U8G2 + OLED(Page/Seg) + BMP/GIF
* [x] I2C/DCMI (RGB565, not JPEG)
* [x] GD25Q512（64KByte) + NES_F407
---
* [ ] JDBC/Redis + InfluxDB/Grafana
* [ ] Flutter + VUE/Spring
* [ ] Rust (Slint) + C# (Unity3D/Cocos2d)

## 2. HAL库的坑
(1) GetFlag和GetITSource \
&emsp; 前者查SR，可以判断中断或事件是否发生，而后者查CR，并不准确掌握状态，只适用单类型中断情况。（CR只会查使能） \
[相关资料](https://shequ.stmicroelectronics.cn/thread-631088-1-1.html "资料") 
 
(2) 复用GPIO默认不上拉（干扰模拟信号） \
&emsp; 有概率浮空串入干扰信号，如从I2S和UART进入干扰模拟信号。

## 3. ExFlash相关
1. 写入操作必须查BUSY或delay，跨页/区（Page/Sector）依赖于第一个ADDR所处的页空间盈余，其他的区自动对齐，统一操作。
2. AT24C02(EEPROM) 类似 \
[W25Q128手册](https://www.winbond.com/resource-files/W25Q128JV%20RevJ%2004252024%20Plus.pdf) 
[AT24C02手册](https://ww1.microchip.com/downloads/en/DeviceDoc/AT24C01C-AT24C02C-I2C-Compatible-Two-Wire-Serial-EEPROM-1Kbit-2Kbit-20006111A.pdf)  
W25Q128 (16MB) **256Byte/Page** \
4KB/Sector \
AT24C02 (2KB) 
**8Byte/Page**

## 4. FATFS避坑
1. FAT12/16/32统称FATFS，其中FAT32不支持小于512MB的设备（如GD25Q128 16M<512M）
2. FatFS的IOCTL接口，注意FRESULT返回值（见"USER_DISKIO.c")

## 5. USB_MassStorage
只需要实现usbd_storage_if.c中的容量、读取、写入块函数即可
问题检查步骤：
1. SDIO 初始化使用1bit总线宽度
2. GetCapacity函数的BlockNbr,BlockSize
3. Write/Read hander伴随CardState模式查忙
4. USBD大容量信息包不超过512bytes，丢包
(注) 一般情况下，SD卡以512Byte为一个Block大小

## 6. SSD1306 OLED相关
尺寸: 128x64，分为8Page，128段，每段为一个字节。
写入的思路：建立buffer缓冲区，以命令选中页，按行列比较每个字节的bit位，决定是否要绘制（如U8G2/NWatch)

## 7. 有关I2S缓冲区组包端序
前提：在WAV文件中读到小端序的字节组，分为24位/16位情况 \
&emsp; 16位情况：I2S的发送器会将16bit直接转大端MSB \
&emsp; 24位情况：I2S的组包被切分成两端，如: \
&emsp;	0x112233 (Value) \
&emsp; 33 22 11 (文件流) \
&emsp; 发送第一包：11 22 （缓冲区存下22 11）\
&emsp; 发送第二包：33 00 （缓冲区存下00 33）\
原因：发送器获得一个int16后，会以八位一组进行大小端转换（在发送序上看）\
推导：假定p为文件流指针 \
&emsp;	低<-- p[0] p[1] p[2] -->高  
&emsp;	实际包：p[2] p[1] || p[0] 00 \
&emsp;	缓存包：p[1] p[2] || 00 p[0] 
我们将缓存包组好后，由DMA送到I2S从机

## 8. DMA双重缓冲区（乒乓缓存，避免取和存对冲）
开辟两个buffer，大小最好是最大传输大小的两倍以上。在DMA传输完成中断后，切换当前缓冲区。对于F407自带MultiBuffer，其他型号考虑DMA半中断切换。	\
常规DMA申请（标准库）：
DMA三要素 -> DMA中断/事件使能 -> DMA_NVIC优先级裁决 -> DMA请求允许
外设使能 -> LinkDMA联系 -> 循环/单次正常模式

## 10. FSMC-NBL[1:0] 奇偶地址
推荐读物：《8086微机原理》\
16位半字的高低位分开，奇地址无法一个周期访存，分两次加掩码（高/低位有效）获取16位完整数据。\
STM32的FMC配置为16bit时，NBL1的信号变化规律记录：\
1.当进行单字节操作，地址为偶地址时，NBL0低电平，NBL1会是高电平，\
案例代码：*(uint8_t *)(0x600000000)=0xaa;\
2.当进行单字节操作，地址为奇地址时，NBL0高电平，NBL1低电平，\
案例代码：*(uint8_t *)(0x600000001)=0xaa;\
3.当进行双字节操作，地址为偶地址时，NBL0、NBL1会是低电平，\
案例代码：*(uint16_t *)(0x600000002)=0xaabb;\
4.当进行双字节操作，地址为奇地址时，相当于分为2次操作(下有说明)。\
案例代码： * (uint16_t*)(0x600000005)=0xaabb;\
相当于操作1：*(uint8_t *)(0x600000005)=0xbb;\
相当于操作2： *(uint8_t *)(0x600000006)=0xaa\

读取操作不用特别控制NBL，默认拉低NBL[1:0]，APB总线会根据地址奇偶从（实际上以字地址）SRAM读出的16位数据中，判断需要该半字的高位/低位。

## 12. ARM汇编 
1. __ASM__()用法：导出C标号别名；寄存器绑定register变量；间接寄存器访问（c标号参加汇编）
2. 跳转：\
	B/BL label的范围32M，基于pc计算与label的距离（相对跳转）；\
	LDR pc,=xxx的范围4Kb，编译时把label的链接地址存在段内某处，LDR载入这个label的编译地址会跑飞（属于绝对跳转）。 \
	连接地址不等于加载地址，导致了这个问题

## 13. RCC外部晶振的必要性
内部HSI RC时序波动，会干扰SPI/I2C/CAN硬件外设的正常CLK建立，另外频率不能太低。

## 14. MPU6050移植流程
* 修改I2C驱动内的MPU_ADDR
* WHOAMI寄存器恒等于0x68 (AD0无关)
* DMP解算库中hw_s的addr修改

## 15. 结构体/变量存放位置
* 全局/静态变量：初始化为0
* 局部变量（栈）：初值随机




